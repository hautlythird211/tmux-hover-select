# tmux-hover-select: select pane on mouse hover
#
# Usage:
#   run-shell "/path/to/hover-select.sh &"
#
# Or toggle with a key binding:
#   bind S run-shell "pgrep -f hover-select >/dev/null && pkill -f hover-select || /path/to/hover-select.sh &"
